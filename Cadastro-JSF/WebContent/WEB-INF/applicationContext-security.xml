<?xml version="1.0" encoding="UTF-8"?>

<b:beans xsi:schemaLocation="http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd 
	http://www.springframework.org/schema/security 
	http://www.springframework.org/schema/security/spring-security-3.1.xsd" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:b="http://www.springframework.org/schema/beans" 
	xmlns="http://www.springframework.org/schema/security">


	<global-method-security secured-annotations="enabled"/>


	<http use-expressions="true" access-denied-page="/error/acessoNegado.xhtml" auto-config="true">

	<intercept-url method="POST" access="hasAnyRole('ADMIN')" pattern="/cadastro/cad_permissao.jsf"/>	
	<intercept-url access="isAuthenticated() and hasAnyRole('USER')" pattern="/resources/components/**"/>
	<intercept-url access="isAuthenticated() and hasAnyRole('USER')" pattern="/resources/**"/>
	<intercept-url access="isAuthenticated() and hasAnyRole('USER')" pattern="/restrito/**"/>
	<intercept-url access="isAnonymous() or isAuthenticated() or !isAuthenticated()" pattern="/error/**"/>
	<intercept-url access="isAuthenticated() and hasAnyRole('USER')" pattern="/templates/**"/>
	<intercept-url access="isAuthenticated() and hasAnyRole('USER')" pattern="/cadastro/**"/>
	<intercept-url access="permitAll" pattern="/publico/**"/>
	<form-login authentication-failure-url="/publico/login.jsf?login_error=1" default-target-url="/restrito/principal.jsf" 
		always-use-default-target="true" login-page="/publico/login.jsf"/>
		<logout delete-cookies="JSESSIONID" logout-url="/j_spring_security_logout" logout-success-url="/publico/login.jsf" 
			invalidate-session="true"/>

	<remember-me token-validity-seconds="1209600"/>


	<session-management session-fixation-protection="migrateSession" invalid-session-url="/publico/login.jsf">
		<concurrency-control error-if-maximum-exceeded="true" expired-url="/publico/login.jsf" max-sessions="1"/>
	</session-management>

</http>


	<authentication-manager>
		<authentication-provider>
			<password-encoder hash="plaintext"/>
			<jdbc-user-service data-source-ref="springDataSource" 
			authorities-by-username-query="select u.ent_login as username, 
											p.esa_codigo as authority from entidade u join entidadeacesso p 
											on u.ent_codigo = p.ent_codigo where u.ent_inativo = false and 
																			upper(u.ent_login) = upper(?)" 
			users-by-username-query="select ent_login as username, 
									ent_senha as password, 'true' as enable from entidade 
									where ent_inativo = false and upper(ent_login) = upper(?)" />
	</authentication-provider>

	</authentication-manager>

</b:beans>